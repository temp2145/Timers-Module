using Blish_HUD.Controls;

namespace temp.Timers.Controls {
    public class AlertContainer : FlowPanel {

        protected override CaptureType CapturesInput() {
            return CaptureType.None;
        }
    }
}
